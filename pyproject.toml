[project]
name = "session-browser"
version = "0.1.0"
description = "Browse and search Claude Code session transcripts"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]

[project.scripts]
session-browser = "server:main"
